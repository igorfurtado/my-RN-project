import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    borderWidth: 1,
    borderColor: '#888',
    borderRadius: 8,
    height: 48,
    paddingHorizontal: 16,
    color: '#333'
  },
  inputFocused: {
    borderColor: '#222'
  }
})
